[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "uq_toolbox"
version = "0.1.0"
description = "Uncertainty Quantification Toolbox for Text Mining"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # Aggiungi qui le tue dipendenze principali (es. transformers, datasets, torch, ecc.)
]

[tool.setuptools.packages.find]
include = ["uq_toolbox*"]
